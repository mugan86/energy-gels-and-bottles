import { SelectLabelFromKeyPipe } from './select-label-from-key.pipe';

describe('SelectLabelFromKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectLabelFromKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
